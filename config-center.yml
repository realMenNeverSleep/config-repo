# 哈哈哈哈
# 嘿嘿嘿嘿
# 不是吧，就这啊！！！
spring:
  profiles:
    active: dev

server:
  port: 8201

spring:
  profiles: dev
  application:
    name: config-server-dev

spring:
  profiles: dat
  application:
    name: config-server-dat
