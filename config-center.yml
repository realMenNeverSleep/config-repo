# 哈哈哈哈
# 嘿嘿嘿嘿
spring:
  profiles:
    active: dev

server:
  port: 8201

spring:
  profiles: dev
  application:
    name: config-server-dev

spring:
  profiles: dat
  application:
    name: config-server-dat
